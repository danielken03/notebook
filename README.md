# this uses react, node, and vite. index.html is used by react to enter the website. package.json and package-lock.json are generated by node using npm so this is not made by me. vite.config.js is created by vite, and tells vite how to use the react plugin. Node_modules are also generated by npm.